<resources>

    <style name="LoggerTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="LoggerTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="LoggerTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>